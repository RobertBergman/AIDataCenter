apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Grafana shares the seed's network namespace, so Prometheus is on loopback.
    url: http://127.0.0.1:9090
    isDefault: true
    editable: false
