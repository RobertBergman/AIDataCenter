apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Replace with vendored Knative manifests + rawDeployment mode preference.
# Day-1: KServe RawDeployment avoids full Knative if preferred — see kserve.yaml.
resources: []
